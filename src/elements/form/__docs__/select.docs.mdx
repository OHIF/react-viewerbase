---
name: Form / Select
menu: Elements
route: /elements/form-select
---

import { Playground, Props } from 'docz';
import { State } from 'react-powerplug'
import { Select } from './../index.js';

# Select

## Basic usage

<Playground>
    <State initial={{ title: 'Test Title', align: 'left',
        list: [{
                title: 'Hello World',
            }]
    }}>
        {({ state, setState }) => (
            <Select {...state} />
        )}
    </State>
</Playground>

## API

<Props of={Select} />